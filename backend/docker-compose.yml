version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    working_dir: /app/backend
    command: python -m backend.app
    ports:
      - "5000:5000"
    restart: unless-stopped
